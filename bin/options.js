// @ts-check

const options = {
  webServerPort: 1212,
  useCompServerInOwnTab: true, // If this is false, first build will not run
  allowFirstBuild: true,
  compilationServerPort: 8000,
  displayNotification: true, // Mac only at the moment
};

export default options;
